<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DotNetMauiOnlineShop.ViewModels"
             xmlns:model="clr-namespace:DotNetMauiOnlineShop.Models"
             x:Class="DotNetMauiOnlineShop.Views.CartPage"
             Title="Shopping Cart">

    <Grid RowDefinitions="*, Auto">
        <CollectionView Grid.Row="0" ItemsSource="{Binding CartItems}" SelectionMode="None">
            <CollectionView.EmptyView>
                <Label Text="Your cart is empty" HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:CartItem">
                    <Frame Margin="10" Padding="10" CornerRadius="5" HasShadow="True">
                        <Grid ColumnDefinitions="*, Auto, Auto">
                            <StackLayout Grid.Column="0">
                                <Label Text="{Binding ProductName}" FontAttributes="Bold" />
                                <Label Text="{Binding Price, StringFormat='{0:C}'}" TextColor="Gray" />
                            </StackLayout>
                            <Label Grid.Column="1" Text="{Binding Quantity, StringFormat='x {0}'}" VerticalOptions="Center" Margin="10,0" />
                            <Label Grid.Column="2" Text="{Binding Total, StringFormat='{0:C}'}" FontAttributes="Bold" VerticalOptions="Center" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Grid Grid.Row="1" ColumnDefinitions="*, Auto" Padding="20" BackgroundColor="{AppThemeBinding Light=White, Dark=Black}">
            <Label Grid.Column="0" Text="{Binding TotalAmount, StringFormat='Total: {0:C}'}" 
                   FontAttributes="Bold" FontSize="20" VerticalOptions="Center" />
            <Button Grid.Column="1" Text="Place Order" Command="{Binding PlaceOrderCommand}" />
        </Grid>

        <ActivityIndicator Grid.Row="0" Grid.RowSpan="2" IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" 
                           HorizontalOptions="Center" VerticalOptions="Center" Color="Blue" />
    </Grid>
</ContentPage>
